version: '3.7'
services:
  # Snapshot
  snapshot:
    build: .
    container_name: snapshot
    env_file:
      - .env
  
    environment:
      VITE_HUB_URL: http://localhost:3000
      VITE_DEFAULT_NETWORK: 11155111
      VITE_IPFS_GATEWAY: snapshot.4everland.link
      VITE_GUARD_URL: http://localhost:8090
      
    ports:
      - '8080:8080'

networks:
  default:
    external: true
    name: snapshot-network
